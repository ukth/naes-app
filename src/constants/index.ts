import { colors } from "./colors";
import messages from "./messages";
import { ACCESS_TOKEN_KEY, REFRESH_TOKEN_KEY, USER_KEY } from "./storageKeys";
import { MIN_TS, HOUR_TS, API_URL, WEB_URL } from "./values";
import styles from "./styles";

export {
  colors,
  messages,
  API_URL,
  ACCESS_TOKEN_KEY,
  REFRESH_TOKEN_KEY,
  USER_KEY,
  MIN_TS,
  HOUR_TS,
  WEB_URL,
  styles,
};
